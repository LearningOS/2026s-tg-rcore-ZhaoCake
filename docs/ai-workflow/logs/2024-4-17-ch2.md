# ch2 (04-17)

既然第一章的都理解了，那么我就直接开第二章。

第二章名叫批处理系统。很显然，这里要加载多道程序了。那么我们对比与之前的代码。

那么我自己去对比需要不少的知识，我让AI给我列出需要明确理解的那部分不同作为TODO。什么叫做需要理解呢？
因为很显然有一部分代码是为核心代码服务的，一旦理解了关键逻辑，其他差异会出现是自然的，所以要抓核心

请AI为我列出核心对比TODOList：

- [x] 启动入口链路：确认 ch1/ch2 都从 `_m_start -> _start -> rust_main`，并说明 ch2 仍复用该主链路的原因。
- [x] 启动栈段位变化：对比 ch1 的 `.bss.uninit` 与 ch2 的 `.boot.stack`，解释为什么 ch2 需要把启动栈从 BSS 分离。
- [ ] `rust_main` 主流程变化：从“打印并关机”升级为“初始化子系统 + 批量加载应用 + Trap 循环 + 统一收尾关机”。
- [ ] 构建脚本职责变化：对比 `build.rs` 从“仅生成链接脚本”变成“生成链接脚本 + 构建用户程序 + 生成 `APP_ASM`”。
- [ ] 用户程序打包机制：看清 `global_asm!(include_str!(env!("APP_ASM")))` 与 `.incbin` 的协作关系。
- [ ] 上下文切换机制：重点观察 `LocalContext::user`、`ctx.execute()`、`sp` 写入，理解 U/S 特权级切换在代码中的落点。
- [ ] Trap 分发逻辑：对比 ch1 无 Trap 分发与 ch2 按 `scause` 分支处理的差异。
- [ ] syscall ABI 约定：核对 `a7`(syscall id)、`a0-a5`(参数)、`a0`(返回值) 在 `handle_syscall` 中的对应代码。
- [ ] `sepc += 4` 的必要性：确认 `ctx.move_next()` 的语义，说明为何必须跳过 `ecall` 指令。
- [ ] syscall 实现边界：观察 `impls` 中 `IO::write` 与 `Process::exit`，理解“分发层 vs 具体实现层”的职责分离。
- [ ] 异常与退出策略：对比 ch1 的单路径退出与 ch2 的“unsupported syscall/异常杀死当前 app，继续下一 app”。
- [ ] 指令缓存一致性：理解 `fence.i` 在“重复复用装载区域”场景下的作用。
- [ ] 配置项变化：对比 `.cargo/config.toml` 新增的 `TG_USER_*` 环境变量，并记录它们如何影响构建行为。
- [ ] 运行现象核验：完成一次 `cargo run` 输出对照，记录“load appN / user output / exit code”关键日志。
- [ ] 测试核验：完成 `./test.sh`，将通过项与失败项（若有）映射到上面的具体机制。

建议记录格式（每项都补）：

- 证据位置：
- 我的理解（一句话）：
- 还没想通的问题：

这更加让我想要设计一种类org的东西，很显然大纲式更适合这种场景。我都有点想直接用org格式了。但是很明显org格式需要依赖于编辑器，我们能不能找到一种更好的解决办法，比如直接按照编程语言的方式，使其自动具备代码块收缩与展开的特征，复用函数块等展开的快捷键。

## 启动入口链路

这是很自然的。如果要跑多任务，那么也应该是在S模式跑，当然就是要在S模式各个初始化步骤做完才开始调度，所以当然_start要跑完，进入到rust_main当中。

> 理解正确。证据：ch1/ch2 的 `_start` 函数签名和 `ENTRY(_m_start)` 完全一致，`rust_main` 仍是 S 模式的唯一入口。ch2 只是把 `rust_main` 的函数体从"打印+关机"扩展为"初始化子系统 + 批量调度"，主链路本身没有变。你说的"S 模式初始化步骤做完才开始调度"就是 `rust_main` 开头那段 `clear_bss()` + `console::init()` 的作用。

## 启动栈段位变化 (04-19)

其实这里有点忍不住想让AI帮我直接答，但我怕一开头就忍不住了，所以还是自己定位吧。
首先是考虑这个出现在哪儿，绝对是build.rs里面，先找到它。但我尽然没有直接找到，但是这个文件里面有很多别的辅助函数。此外，找到一个write_linker函数，从这个函数来看，似乎直接用了tg_linker::NOBIOS_SCRIPT的这个变量。我过去看看。
在`tg-rcore-tutorial-linker/src/lib.rs`的84行找到了这个变量。注意到在M模式下都是一样的，区别是S模式下。这个新的NOBIOS的链接脚本中，.boot是在.bss后4k对齐的一个新段，里面有stack。而旧的ch1的中，这个uninit是应该是在.bss之前的。出现这样的情况，我暂时只能理解为给栈留出更多空间。但是这个机制，可能跟我还不够理解linker的语法有关。此外，我应该读带啊去找到。

> **评价你的理解**：
>
> 你定位到了关键文件的路径正确（`tg-rcore-tutorial-linker/src/lib.rs`），也注意到了 M 模式段没变、S 模式段有差异，观察方向是对的。但有两个点需要修正：
>
> 1. **ch1 里没有 `.boot` 段**——ch1 的链接脚本直接内联在 `build.rs` 里（第 39-75 行），`.bss` 里只有 `*(.bss.uninit)` 和 `*(.bss .bss.*)`，启动栈放在 `.bss.uninit`。ch2 引入的 `NOBIOS_SCRIPT`（以及 `SCRIPT`）都多了一个 **`.boot` 段放在 `.bss` 之后**，专门存放 `KEEP(*(.boot.stack))`。所以核心区别不是"uninit 在 .bss 前/后"，而是 **ch2 新增了一个独立于 .bss 的 .boot 段来放启动栈**。
>
> 2. **原因不是"给栈留更多空间"**——真正的原因是 **ch2 的 `rust_main` 会执行 `zero_bss()` 清零 BSS 段**。如果启动栈仍然放在 `.bss.uninit`（属于 .bss 段），清 BSS 就会把正在使用的内核栈内容归零，导致崩溃。因此必须把启动栈移到 `.boot` 段——它在 `.bss` **之后**，`zero_bss()` 只清 `[__sbss, __ebss)` 范围，不影响 `.boot` 区域。（证据：`tg-rcore-tutorial-linker/src/lib.rs` 第 116-120 行 `.bss` 段定义了 `__sbss` 和 `__ebss` 边界；第 121-124 行 `.boot` 段用 `KEEP` 保护且有独立 `__boot` 边界。）
>
> ---
>
> **关于 `KEEP` 的作用**：
>
> 链接器在 GC（garbage collection of sections）阶段会自动移除"没有任何引用指向的段"。启动栈 `STACK` 是一个 `static mut` 局部变量，Rust 代码不会通过符号引用它——只有 `_start` 的 naked asm 通过 `la sp, {stack}` 间接引用。链接器的引用分析看不到这种引用，会认为 `.boot.stack` 段是"没人用的"，把它删掉。
>
> `KEEP(*(.boot.stack))` 的作用就是**告诉链接器：即使看起来没人引用这个段，也不要删它**。这是链接脚本层面的"防 GC 保护"。类似地，如果你在链接脚本里写了 `KEEP(*(.text.entry))`，即使没有明文引用 `_start`，入口段也不会被优化掉。
>
> 对比：ch1 不需要 KEEP，因为它的链接脚本里直接通配 `*(.bss .bss.*)`，uninit 栈碰巧不会被 GC 掉（.bss 段整体被保留）；而 ch2 把栈单独放在一个命名段 `.boot.stack`，就必须用 KEEP 显式保护。

确实，我看到了ch2中`main.rs:80`会清零bss段，因此不能在把boot段留在bss内了。

